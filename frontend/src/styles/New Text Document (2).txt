.chat-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: linear-gradient(
    180deg,
    #FAFAFC 0%,
    #F5F5F7 100%
  );
}

.chat-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18px 28px;
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.95) 0%,
    rgba(248, 246, 252, 0.9) 100%
  );
  border-bottom: 1px solid rgba(232, 232, 237, 0.8);
  box-shadow: 
    0 4px 24px rgba(155, 127, 232, 0.08),
    0 0 0 1px rgba(155, 127, 232, 0.05);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}

.header-left {
  display: flex;
  align-items: center;
  gap: 14px;
}

.header-left h1 {
  font-size: 20px;
  background: linear-gradient(135deg, #3A3A3C 0%, #5B5B5F 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0;
  font-weight: 600;
}

.version-badge {
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.85) 0%,
    rgba(184, 167, 232, 0.8) 50%,
    rgba(201, 184, 240, 0.75) 100%
  );
  color: white;
  padding: 6px 14px;
  border-radius: 20px;
  font-size: 12px;
  font-weight: 600;
  box-shadow: 
    0 4px 16px rgba(155, 127, 232, 0.3),
    0 0 0 1px rgba(255, 255, 255, 0.2) inset;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.header-right {
  display: flex;
  align-items: center;
  gap: 10px;
  color: #6B5B9E;
  font-size: 13px;
  font-weight: 600;
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.1) 0%,
    rgba(184, 167, 232, 0.08) 100%
  );
  padding: 8px 14px;
  border-radius: 20px;
  border: 1px solid rgba(155, 127, 232, 0.15);
}

.messages-container {
  flex: 1;
  overflow-y: auto;
  padding: 28px;
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.message {
  display: flex;
  max-width: 75%;
}

.message.user {
  align-self: flex-end;
}

.message.bot {
  align-self: flex-start;
}

.message-content {
  padding: 18px 22px;
  border-radius: 16px;
  line-height: 1.6;
  font-size: 15px;
}

.message.user .message-content {
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.92) 0%,
    rgba(184, 167, 232, 0.88) 50%,
    rgba(201, 184, 240, 0.85) 100%
  );
  color: white;
  border-bottom-right-radius: 6px;
  box-shadow: 
    0 8px 24px rgba(155, 127, 232, 0.35),
    0 0 0 1px rgba(255, 255, 255, 0.15) inset;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.message.bot .message-content {
  background: rgba(255, 255, 255, 0.95);
  color: #3A3A3C;
  border: 1px solid rgba(232, 232, 237, 0.8);
  border-bottom-left-radius: 6px;
  box-shadow: 
    0 4px 16px rgba(155, 127, 232, 0.08),
    0 0 0 1px rgba(155, 127, 232, 0.05);
  backdrop-filter: blur(10px);
}

.message-meta {
  margin-top: 14px;
  padding-top: 14px;
  border-top: 1px solid rgba(232, 232, 237, 0.6);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.confidence {
  font-size: 12px;
  color: #86868B;
  background: rgba(155, 127, 232, 0.08);
  padding: 6px 12px;
  border-radius: 10px;
  border: 1px solid rgba(155, 127, 232, 0.12);
}

.feedback-buttons {
  display: flex;
  gap: 8px;
}

.feedback-buttons button {
  background: rgba(255, 255, 255, 0.8);
  border: 1px solid rgba(232, 232, 237, 0.8);
  border-radius: 8px;
  padding: 8px;
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  color: #6E6E73;
}

.feedback-buttons button:hover {
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.12) 0%,
    rgba(184, 167, 232, 0.1) 100%
  );
  border-color: rgba(155, 127, 232, 0.3);
  color: #6B5B9E;
  transform: translateY(-1px);
}

.input-container {
  padding: 28px;
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.95) 0%,
    rgba(248, 246, 252, 0.9) 100%
  );
  border-top: 1px solid rgba(232, 232, 237, 0.8);
  box-shadow: 
    0 -4px 24px rgba(155, 127, 232, 0.06),
    0 0 0 1px rgba(155, 127, 232, 0.04);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}

.input-box {
  display: flex;
  gap: 12px;
  max-width: 800px;
  margin: 0 auto;
}

.input-box input {
  flex: 1;
  padding: 18px 24px;
  border: 1.5px solid rgba(232, 232, 237, 0.9);
  border-radius: 16px;
  font-size: 16px;
  outline: none;
  background: rgba(255, 255, 255, 0.9);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 
    0 2px 8px rgba(155, 127, 232, 0.05),
    0 0 0 1px rgba(155, 127, 232, 0.03);
}

.input-box input:focus {
  border-color: rgba(155, 127, 232, 0.5);
  box-shadow: 
    0 0 0 5px rgba(155, 127, 232, 0.12),
    0 8px 32px rgba(155, 127, 232, 0.25);
  background: rgba(255, 255, 255, 1);
}

.input-box button {
  padding: 18px 28px;
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.9) 0%,
    rgba(184, 167, 232, 0.85) 50%,
    rgba(201, 184, 240, 0.8) 100%
  );
  color: white;
  border: none;
  border-radius: 16px;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 
    0 8px 24px rgba(155, 127, 232, 0.35),
    0 0 0 1px rgba(255, 255, 255, 0.25) inset;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.input-box button:hover:not(:disabled) {
  transform: scale(1.05);
  box-shadow: 
    0 12px 32px rgba(155, 127, 232, 0.45),
    0 0 0 1px rgba(255, 255, 255, 0.3) inset;
}

.input-box button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

.disclaimer {
  text-align: center;
  font-size: 12px;
  color: #86868B;
  margin-top: 14px;
  background: rgba(155, 127, 232, 0.06);
  padding: 10px;
  border-radius: 10px;
  border: 1px solid rgba(155, 127, 232, 0.1);
}

.typing-indicator {
  display: flex;
  gap: 6px;
  padding: 8px 0;
}

.typing-indicator span {
  width: 9px;
  height: 9px;
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.8) 0%,
    rgba(184, 167, 232, 0.7) 100%
  );
  border-radius: 50%;
  animation: bounce 1.4s infinite;
  box-shadow: 0 2px 8px rgba(155, 127, 232, 0.3);
}

.typing-indicator span:nth-child(2) {
  animation-delay: 0.2s;
}

.typing-indicator span:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes bounce {
  0%, 60%, 100% { transform: translateY(0); }
  30% { transform: translateY(-10px); }
}

/* Scrollbar Styling */
.messages-container::-webkit-scrollbar {
  width: 8px;
}

.messages-container::-webkit-scrollbar-track {
  background: rgba(232, 232, 237, 0.3);
  border-radius: 4px;
}

.messages-container::-webkit-scrollbar-thumb {
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.4) 0%,
    rgba(184, 167, 232, 0.35) 100%
  );
  border-radius: 4px;
}

.messages-container::-webkit-scrollbar-thumb:hover {
  background: linear-gradient(
    135deg,
    rgba(155, 127, 232, 0.55) 0%,
    rgba(184, 167, 232, 0.5) 100%
  );
}